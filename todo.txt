[x] dirty load the chapter files to check whether the grouping looks and feels as expected
[x] write files actually in the same folder as the source files under the current datetime so that it can't colide with anything.
[x] create a settings file (in .config ?)
    and a default save location ? or should it ask explicitly if it never asked before?
    if I would go with a very automaticized way then the decision whether onedrive or not could be called `preferableOnderive`
    - benefit here would be that I don't have to deal with rogue generated files in my changelist

[x] translation problem with words like - outer vs inner translations
    (outer = top level translations | inner = meaning translations)
    examples:
    - nimisõna: outer - some | inner - some
    - sõbranna: outer - none | inner - 1 eng
    subtasks:
    [x] some have no top level translations but in the meanings they do
    [x] then there are words where the opposite is true - ,
    [x] then there are those which have both.
    [?] then which have none..., all of these cases need handling.
    - started using ekilex

[x] (sonapi) create new translation loading algo which would read the meaning translations rather than the top list
    - irrelevant since I switched to ekilex

[x] maybe deprecate the current `SonapiResponse.WordFormValues` function and make it take a `SearchResult` since that is the only thing it is currently
    operating upon.
    - started using ekilex

[x] parallelize ekilex loading
  - parallelization was not really possible just like with Sõnaveeb, but implemented async/threading solution so that the UI is not frozen.
  - implemented also a live communication where it writes into the OutputTextBox what the currently loaded word is.

[-] figure out whether I want the extra data: page, Tere translation, etc.
    transfered as well
    [-] if done => compare the Tere translations with the collected meaning translations
      - this is not a task to be done here but one that needs to be done everytime when I generated cards with this program.
    - probably the chapter number from the file name should become a tag
      - done
    - maybe if the file is found in multiple file (thus multiple chapters - bluebox, or other), then all where it was present shall be added.
      - yeah but maybe I will write a file, generate it, and a month later would maybe write the same word into another file, right?
        being able to just collect from multiple files at that point would be pointless, it would already be in anki.

[-] maybe create a cache (maybe of only words with wordclass) and if a word has data there 
    (like wordclass) then load it from there otherwise go to sonapi.
    - this just gained priority because even with the added sleep-s sometimes I still get garbage 
    - garbage seems to be differentiable from those which just lack info by the absence of the requestedWord field - which thus for me manifests as null.
  - no cache needed, sometimes it is annoying when using from the train, but last time I used the wifi of the train and it worked fine!
    definitely would lead to complexity that currently I don't think I need.

[x] solve drawio file being annoying
  - moved out the drawio file into the outer folder while the outer folder is now a wrapper for this project.
  - a script copies every file from the wrapper folder to a predefined onedrive location
  - in other words I am now backing up non-repo files to onedrive with a script and storing them outside of the repo entirely.

[x] add the level (A1,A2,B1,...) to the tags.

[x] if a word or phrase was not found a warning is made so that the user can determine what went wrong.
  - Restructure logic in EkilexView to ask for the wordId-s of a word and then for details of each wordId.
    If no wordId-s found, report the word (and wordId?).
    If found wordId-s but none resulted in a CardData, report the word (and wordId?).
  - Till now we were first loading all the wordId-s of all the words, but here we are first fully processing a word before continueing to the next word.

[x] introduce ability to ekilex based solution to generate files as the sonapi based variant was able to.

[x] connect to the anki database so that we can detect if a word is already there 

[x] limit the example count?
    decided to not limit too much the example count, currently taking 10, later will make it configurable.
    Also changed the example collection logic to ensure some non-short example are collected
    (depends on the value, if 0 then we just collect based on sentence word count).

[x] homonym id-s currently are the same which causes them to be flaged as duplicates
    I could dissambiguate by adding the first english meaning to the id - which is only displayed in the browser.
    - I have added the wordId to the Id field of CardData after the word itself which is now used for checking whether the wordId is already in the DB and skip if it is.

[x] currently profileName, tag/-s, example limit, etc., are hard coded values, make these configurable either via settings or with UI - or both
    (i.e. changing UI will change settings and when opened will load and add value to UI).

[x] failure reason for processing a wordId is currently not present in UI, nor is it transfered in code.
    Somehow should be able to tell the user why the given wordId failed.

[x] fix bug where "blank" tag is added somehow implicitly
  - The tag was added by anki because we generated the tag "generated::" when the proficiencyLevel was nothing.
  - Now we are adding levels as for example: "generated::level::A1"; and explicitly adding "generated::level::none".

--- Idea
    Shouldn't it rather be better if we would not just use generic forms like form1, form2, form3, etc
    but rather have a field for Nsg, Gsg, ..., ma, da, tud-dud, ...?
    - noOO!!!! then I would have a brutal number of fields, I already have a lot of fields! 

[x] For verbs I think (later gemini confirmed more or less)
    form1(ma     ) form4(da,ta,a    )
    form2(olevik ) form5(lihtminevik)
    form3(tud,dud) form6(nud        )
    maybe I need the impersonal(umbisikuline)? (-dakse, -takse, -akse.)
    - gemini says that I don't need the umbi because it is 99% of the time derivable from the (tud,dud)
    - on sõnaveeb in the top right little table, the forms which they are showing there I assume ar the most important ones
      whiche are:
        ma, da, sg3p, tud-dud, lihtminevik, nud, imperative, 
[x] write logs from textbox into a file!

[x] add the word classes somehow? because at the moment locating all the muutumatu is not possible neither with tags nor with text search...
  - added PoF and wordClass as tag.

----------------------------------------------------------------------------------------------------

[ ] handle duplicate words while loading
    - duplicates of the same file
    - duplicates accross multiple files when loading from multiple files
[ ] when loading words which are already in Anki, detect if the current occurence would add a new tag, and report what tag would be added.
    (Not sure whether this needs to be a different "button" i.e. maybe it would cause a lot of time, thus should be a separate feature
    that runs independently from the main load button)

